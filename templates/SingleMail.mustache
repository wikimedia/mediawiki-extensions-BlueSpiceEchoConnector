<html>
	<head>
		<meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<style>
			body {
				background-color: #fcfcfc;
				font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
			}

			p {
				margin-top: 0;
			}

			ul {
				margin-top: 0;
				margin-bottom: 0;
			}

			h1,h2,h3,h4,h5,h6 {
				margin-top: 0;
				margin-bottom: 5px;
			}

			.mail-container {
				width: 80%;
				max-width: 600px;
				margin-left: auto;
				margin-right: auto;
				background-color: #e7e7e7;
				padding: 30px;
			}
			.intro-header {
				position:relative;
				width: 80%;
				max-width: 600px;
				margin-left: auto;
				margin-right: auto;
				background-color: #3e5389;
				padding-top: 15px;
				padding-bottom: 15px;
				padding-left: 30px;
				padding-right: 30px;
			}
			.intro-header .sender-info {
				position: absolute;
				right: 10px;
				top: 4px;
				font-size: .8em;
			}
			.intro-header span{
				color: white;
			}
			.actions {
				margin-top: 10px;
			}
			.actions div {
				margin-bottom: 15px;
			}
			.footer {
				padding-top: 15px;
				text-align: center;
				font-size: .8em;
			}
			.content td {
				vertical-align: top;
			}
			.content td.content-text {
				padding-left: 10px;
			}
			.primary-actions ul {
				list-style-type: none;
				margin-top: 20px;
				padding: 0;
			}
		</style>
	</head>
	<body>
		<div class='intro-header'>
			<span>{{greeting}}</span>
			<span class='sender-info'>{{sender-info}}</span>
		 </div>
		<div class='mail-container'>

			<table class='content'>
				<tr>
					<td><img alt="Icon" width="32" src='{{icon_url}}' /></td>
					<td class='content-text'>
						{{#header}}<h3 class="header">{{{.}}}</h3>{{/header}}
						{{{body}}}
						<div class='actions'>
							{{#actions}}
							<div class="primary-actions">
								{{{primary}}}
							</div>
							{{/actions}}
						</div>
					</td>
				</tr>
			</table>
			{{#actions}}
				{{#secondary}}
			<div class='actions'>
				<h4>{{actions.secondary_label}}</h4>
				{{{.}}}
			</div>
				{{/secondary}}
			{{/actions}}
		</div>
		<div class='footer'>{{{footer}}}</div>
	</body>
</html>